<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="base_texte">
        <item name="android:textSize">@dimen/taille_texte</item>
        <item name="android:textColor">@color/couleur_texte</item>
    </style>

    <style name="resultat_texte">
        <item name="android:textSize">@dimen/taille_texte</item>
        <item name="android:textColor">@color/couleur_texte_bouton</item>
    </style>

</resources>
